# 2026-05-23 创建 PHP 工具函数库

创建 `php-predict/functions.php`，包含 7 个通用工具函数。

## 变更文件

- [`php-predict/functions.php`](php-predict/functions.php) — 新建

## 函数清单

| 函数 | 用途 |
|------|------|
| `config($key, $default)` | 点号分隔嵌套配置读取，静态缓存 config.php |
| `db()` | SQLite3 单例，路径来自 `config('db.path')`，启用 WAL + 外键 |
| `init_db()` | 初始化 matches / predictions / prediction_results 三张表 |
| `api_get($path)` | cURL GET 调用 FastAPI (`http://127.0.0.1:8888`)，返回解码数组 |
| `e($str)` | `htmlspecialchars` 安全转义 |
| `money_fmt($n)` | `number_format` 金额格式化（千分位 + 两位小数） |
| `percent_fmt($n)` | 百分比格式化，自动识别小数/百分数形式 |