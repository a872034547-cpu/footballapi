# API 能力升级完成 — 三阶段数据源增强

## 日期
2026-05-23

## 概述
完成三阶段 API 数据源增强，新增凯利指数计算器和亚盘数据采集能力。

## 变更文件

### 新建
- `app/services/kelly_calculator.py` — 凯利指数计算器，基于公式 K=(p×odds-1)/(odds-1) 自行计算，不依赖外部数据源

### 修改
- `app/services/providers/nowscore.py` — 修复 `_fetch_odds` 方法，正确解析 3in1Odds.aspx 的 3 表结构（亚盘/大小球/欧赔），现返回 7 条价格
- `app/services/providers/zgzcw.py` — 新增 `_fetch_asian_detail()` 亚盘水位采集 + `_parse_handicap_line()` 中文盘口解析；`get_match_odds()` 集成亚盘数据
- `app/routes/predict.py` — 新增 `GET /match/{match_id}/kelly` 端点，返回凯利指数及博彩公司明细

## 验证结果
- `/match/demo-001/kelly` 返回 5 家博彩公司凯利明细，各市场凯利值计算正确
- FastAPI 服务运行正常 (port 8888)