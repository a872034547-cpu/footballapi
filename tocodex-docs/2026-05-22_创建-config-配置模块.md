# 配置模块变更记录

## 概述
创建了 [`Settings`](app/config.py:7) 配置模块，基于 `pydantic-settings` 的 `BaseSettings` 实现，支持从 [`.env`](.env) 读取配置并忽略额外字段；同时提供数据源启用状态判断和带缓存的 [`get_settings()`](app/config.py:38)。

## 变更文件
- [`app/config.py`](app/config.py)
